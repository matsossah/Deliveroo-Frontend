import React from "react";
import Button from "./Button.js";

const Cart = (props) => {
  return (
    <div className="cart">
      <Button title="Valider mon panier" />
    </div>
  );
};

export default Cart;
